<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

<div class="container mx-auto mt-10 px-4">
  <div class="bg-white shadow-md rounded-lg p-6">
    <h2 class="text-2xl font-bold text-blue-600 mb-4"><%= counsellor.name %>'s Dashboard</h2>

    <p class="mb-2"><span class="font-semibold">Specialization:</span> <span class="text-gray-600"><%= counsellor.specialization || "N/A" %></span></p>

    <p class="mb-4">
      <span class="font-semibold">Status:</span>
      <% if (counsellor.booked) { %>
        <span class="inline-block bg-red-500 text-white text-sm px-2 py-1 rounded-full">Booked</span>
      <% } else { %>
        <span class="inline-block bg-green-500 text-white text-sm px-2 py-1 rounded-full">Available</span>
      <% } %>
    </p>

    <% if (bookedBy) { %>
      <div class="bg-blue-50 border-l-4 border-blue-500 p-4 rounded-md shadow-sm mb-4">
        <h5 class="font-semibold text-blue-700 mb-2">📌 Booked By:</h5>
        <ul class="space-y-1">
          <li><span class="font-semibold">Name:</span> <%= bookedBy.name %></li>
          <li><span class="font-semibold">Email:</span> <%= bookedBy.email %></li>
          <li><span class="font-semibold">Instagram ID:</span> <%= bookedBy.Instagram || "N/A" %></li>
          <li>
            <span class="font-semibold">Booked Slot:</span> 
            <% if(counsellor.selectedSlot) { %>
              <span class="inline-block bg-blue-500 text-white text-sm px-2 py-1 rounded-full"><%= counsellor.selectedSlot %></span>
            <% } else { %>
              <span class="text-gray-400">N/A</span>
            <% } %>
          </li>
        </ul>
      </div>
    <% } else { %>
      <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded-md shadow-sm mb-4">
        No one has booked this counsellor yet.
      </div>
    <% } %>

    <!-- <a href="/home" class="inline-block mt-4 px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-600 transition">⬅ Back to Home</a> -->
  </div>
</div>
</body>
</html>